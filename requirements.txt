# 在线推理服务依赖（server/app.py）
# 若遇 numpy/scipy 兼容问题，请使用虚拟环境并优先安装：pip install "numpy<2" "scipy>=1.10"
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0
numpy<2
scipy>=1.10
